;(function (window, undefined) {
    'use strict';

    hiocsApp.service("student_newStudentPlacementService", ['$timeout', '$http', '$log', 'app', function($timeout, $http, $log, app) {
        
    }]);

})(window);